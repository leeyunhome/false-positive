# 개발 일지 및 시행착오 기록 (DEV_LOG.md)

**프로젝트명:** 오탐 — FALSE POSITIVE : THALASSA-9  
**최종 업데이트:** 2026-08-09  
**작성 목적:** 프로젝트 개발 과정, 해커톤 피봇, AI 시스템 설계 및 밸런스 검증 과정에서 발생한 시행착오와 해결 내역 기록.

---

## 1. 프로젝트 주요 피봇 (Pivot)

- **상황:** 기존 참가 예정이었던 NHN GAME x AI HACKATHON 2026의 오프라인 본선 참석이 불가능해짐.
- **결정:** **OpenAI GAME BUILDERS SEOUL 2026** (온라인 웜업 2026-08-18 마감)으로 출품 대상을 성공적으로 변경.
- **성과:** 마감 기한을 약 8일 더 확보하면서, 1개 교대(4건)였던 베이크드 경보 데이터를 **2개 교대(8건)의 완전한 2교대 연속 플레이 게임**으로 완성도 있게 확장함.

---

## 2. 핵심 설계 원칙: 정답과 논리의 분리 (`truth` vs `bias`)

### 📌 설계 개념
- **정답 (`truth.isRealThreat`)**: 게임 엔진 및 콘텐츠 정의 파일(`data/incidents.json`)이 결정론적으로 소유함.
- **AI 조언 (`nereus.bias`)**: LLM은 정답 그 자체가 아닌, "지정된 오차 패턴(bias)을 가진 조언의 말투와 논리"만 담당함.
- **이유**: LLM에게 런타임 승패 판정을 맡기면 결정론이 깨지고, 환각(Hallucination)에 의해 게임 밸런싱 및 자동 검증이 불가능해지기 때문.

---

## 3. 시행착오 및 해결 내역 (Trial & Error)

### 3-1. 경보 확장 시 발생한 지배 전략(Dominant Strategy) 결함
- **문제 발생:**
  - 경보를 4건에서 8건으로 확정하는 과정에서 `validate.mjs` 검증 실행 시 실패 에러 발생.
  - 교대당 제공하는 조사 토큰이 4개로 넉넉해지면서 플레이어가 모든 증거를 제한 없이 열람(`전건 최소조사 4`)할 수 있게 되어 자원 관리의 긴장감이 사라짐.
  - 2교대 경보에서 NEREUS의 권고가 한 번도 맞지 않자, 플레이어 입장에서 "NEREUS는 무조건 기각한다"가 지배 전략이 되는 밸런스 결함 발생.
- **해결 방안:**
  - `data/incidents.json`의 교대당 시간 토큰을 **3개**로 타이트하게 설정하여 플레이어가 전략적으로 증거를 선택하도록 유도.
  - NEREUS가 정답을 맞히는 경보(`INC-104`, `INC-203`)를 적절히 섞고 신뢰도 수치와 다르게 배치하여 플레이어가 NEREUS의 편향 유형을 직접 관찰하고 생각하도록 밸런싱 조정 ➔ `validate.mjs` 검증 통과.

### 3-2. 디렉터 레이어가 "구현 완료"로 기록됐지만 실제로는 죽은 코드였던 건
- **문제 발생:**
  - `js/game.js`에 `applyDirectorRules()`가 있고 문서에도 구현 완료로 적혀 있었으나, **`S.activePolicies`에 값을 넣는 코드가 어디에도 없었다.** 규칙 선택 UI가 없어 이 함수는 게임 전체에서 단 한 번도 발동하지 않았다.
  - 더 근본적인 문제: 기존 규칙은 `inc.nereus.bias`(숨은 오차 라벨)를 직접 읽고 있었다. 이는 §2 원칙 위반이다 — 정책이 정답을 훔쳐보면 플레이어가 설계한 것이 아니라 엔진이 대신 푼 것이 되고, 규칙이 절대 빗나가지 않으니 채점할 것도 없어진다.
- **해결 방안:**
  - 규칙 테이블을 `js/policies.js`(DOM 비의존 순수 모듈)로 분리하고, **관측 가능한 특징만** 보도록 전면 재작성 (`reporter.state`, `signals[].label`, `nereus.confidence`).
  - 교대 사이에 **디렉터 콘솔 화면**을 추가, 4개 중 최대 2개를 선택하면 다음 교대의 NEREUS 신뢰도에 실제로 반영되고 브리핑에 발동 배지가 표시되도록 구현.
  - 결산에 **규칙 자체의 혼동행렬**(적중 / 오작동 / 미발동)을 추가. 플레이어의 판단뿐 아니라 플레이어가 설계한 정책이 채점된다.

### 3-3. 규칙 선택이 '선택'이 되지 않는 밸런스 결함
- **문제 발생:**
  - 모든 규칙이 어긋난 권고만 정확히 깎으면 비용이 없어 **"고를 수 있는 만큼 다 켜기"가 지배 전략**이 되고, 디렉터 레이어는 장식이 된다. §3-1의 "무조건 기각" 결함과 같은 종류다.
- **해결 방안:**
  - `tools/validate.mjs`가 `js/policies.js`를 그대로 불러와(`data:` URL import — package.json·빌드 스텝 불필요) 규칙별 적중/오작동을 집계하고, ①오작동 0 ②적중 규칙 0 ③적중 규칙이 선택 한도 이하인 경우를 검증 실패로 잡도록 확장.
  - 콘텐츠를 조정해 넓은 규칙(`신뢰도 0.85 이상 일괄 감산`)이 INC-203의 **옳은 권고까지 깎도록** 배치하고, `격앙 보고 감산`은 교대 2에서 한 번도 걸리지 않는 **과적합 함정**으로 남김.
  - 실측: 정밀 규칙 2개 = 적중 2 / 오작동 0. 넓은 규칙 = 적중 2 / 오작동 1 / 미발동 1. 선택이 실제 트레이드오프가 됨.

### 3-4. 대용량 JSON 데이터 편집 시 텍스트 치환 오류
- **문제 발생:**
  - `incidents.json` 편집 시 일부 교체 툴에서 TargetContent 매칭 실패가 발생.
- **해결 방안:**
  - `write_to_file` 도구를 사용하여 전체 파일 덮어쓰기 방식으로 스키마 및 JSON 데이터의 일관성과 구조적 완전성을 보장함.

---

## 4. 최종 개발 및 배포 상태

1. **게임 런타임**: Pure HTML/CSS/JS (외부 프레임워크 및 빌드 도구 0).
2. **배포**: GitHub Pages 정상 배포 완료 (`https://leeyunhome.github.io/false-positive/`).
3. **원격 저장소**: `https://github.com/leeyunhome/false-positive`
4. **해커톤 제출 파일**: `false-positive.zip` (33KB) 압축 아카이브 생성 완료.

---

### 3-5. 쿼터뷰 배치도 — 연출이 판정을 오염시키지 않게 하기
- **문제 발생:**
  - `조사`가 텍스트 버튼 하나여서 "현장을 본다"는 감각이 없었다. Planescape: Torment 계보의 미니 탐색 요소를 넣고 싶었으나, QTE나 ROV 직접 조작까지 가면 판단 게임의 리듬이 끊기고 아트 에셋 0 제약과도 충돌한다.
  - 좌표를 렌더러 코드에 하드코딩하면 증거와 위치의 대응이 검증 밖으로 빠진다. 디렉터 레이어가 죽은 코드였던 것(§3-2)과 같은 종류의 위험이다.
- **해결 방안:**
  - 범위를 **연출로만** 한정. `조사` 클릭 시 관측 초점이 해당 지점까지 1.15초 이동한 뒤 증거가 공개된다. 토큰은 누른 즉시 차감되고, 맵 클릭으로 건너뛸 수 있다. 판정 로직은 한 줄도 바뀌지 않았다.
  - 좌표를 `js/stations.js`(순수 모듈)로 분리하고 증거 15건 전부에 `site`를 추가, 스키마 필수 필드로 지정. `validate.mjs`가 ①존재하지 않는 지점을 가리키는 증거 ②아무도 찾지 않는 죽은 지점을 검사한다 (현재 10/10 사용).
- **실기 스크린샷을 보고 고친 것 2건:**
  - 라벨이 앞쪽 구조물 박스에 가려 "관제 코어", "거주 구획"이 아예 안 보였다 ➔ 도형을 전부 그린 뒤 라벨만 2패스로 그리도록 분리.
  - 라벨을 지붕 위에 띄우니 어느 구조물의 이름인지 헷갈렸다 ➔ 바닥(발자국) 기준으로 통일.
- **함께 잡은 버그:** 같은 지점을 다시 조사할 때 이동 거리가 0인데도 `isTraveling()`이 1.15초간 참으로 남아 다음 조사가 막혔다. `probeTo()`에서 출발지와 목적지가 같으면 즉시 완료 처리.

---

## 5. 향후 확장 가능 방향

- **시연 영상 제작**: 30~60초 코어 루프 가동 캡처 영상 제출. 남은 항목 중 최우선.
- **AI 활용 기술 문서**: 정답/조언 분리 원칙, 규칙이 정답을 훔쳐보지 못하게 한 설계, `validate.mjs`가 실제로 잡아낸 결함 사례를 근거로 작성.
- **컷 확정 항목** (재제안 대상 아님): QTE·ROV 직접 조작, 라이브 LLM 프록시.
