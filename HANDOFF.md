# HANDOFF — 「오탐 / FALSE POSITIVE : THALASSA-9」

이 문서를 읽는 것만으로 지금까지 대화 맥락 없이 작업을 이어받을 수 있어야 합니다. 코드보다 이 문서를 먼저 읽으세요.

## 0. 지금 상황 (가장 중요)

- **출품 대상 대회:** **OpenAI GAME BUILDERS SEOUL 2026** (기존 NHN 해커톤 오프라인 참석 불가로 피봇 확정)
- **온라인 예선(First Challenge) 마감:** **2026-08-18 (화).** (약 9일의 여유 시간 확보)
- **오프라인 본선:** 2026-09-03 (목) ~ 09-04 (금) (서울 1박 2일 본선 해커톤)
- **개발환경:** 순수 HTML/CSS/JS (빌드 스텝 0, 프레임워크 0). GitHub Pages로 직접 배포/런타임 제공.

## 1. 무엇을 만드는가

**장르:** Papers, Please / Reigns / Orwell 계보의 텍스트 기반 의사결정 시뮬레이션 게임.

**설정:** 수심 3,200m 무인 해저 채굴·연구 기지 THALASSA-9. 야간 당직 관제사(플레이어) 혼자. 온보드 AI **NEREUS**가 경보를 해석해 조치를 권고하지만, **체계적으로(학습 가능한 오차 패턴으로) 틀린다.**

**코어 루프 (2개 교대 진행 = 총 경보 8건):**
1. 경보 수신 (승무원 보고 / 소나 / 센서 / 자동 감지)
2. NEREUS 브리핑 — 진단 + 권고 조치 + 신뢰도 수치 (디렉터 규칙 감산 적용)
3. 플레이어 판단 — 동사 3개: **승인**(비용 0) / **조사**(시간 토큰 소모, 하드 증거 확보) / **기각**(직접 다른 조치 선택)
4. 결과 반영 (선체/생명유지/신뢰 게이지 변화) → 다음 경보
5. 교대 종료 → **혼동행렬**(정탐/오탐/미탐/정상기각)로 결산 ➔ 2교대 연속 진행

## 2. 핵심 설계 원칙 — 절대 어기면 안 되는 것 하나

> **정답(`truth`)은 게임 엔진/콘텐츠 파일이 소유한다. LLM은 "그 정답에 대해 지정된 방향으로 어긋난 조언의 말투와 논리"만 생성한다. LLM이 정답 자체를 결정하게 하면 안 된다.**

이유: LLM이 판정을 하면 (a) 검증 불가능해지고 (b) 결정론이 깨지며 (c) 밸런싱이 불가능해집니다. `schema/incident.schema.json`과 `tools/validate.mjs`가 이 분리와 밸런스를 강제합니다.

## 3. 진행된 작업 및 시행착오 (2026-08-09 업데이트)

### 3-1. 진행 완결 사항
1. **GitHub 원격 저장소 및 배포 확정**
   - 원격 레포: `https://github.com/leeyunhome/false-positive`
   - Live Demo: `https://leeyunhome.github.io/false-positive/` (GitHub Pages 정상 배포 중)
2. **콘텐츠 2배 확장 (4건 ➔ 8건 경보, 2개 교대)**
   - `data/incidents.json`에 `INC-101~104`(교대 1), `INC-201~204`(교대 2) 구성 완료.
3. **다중 교대 구현**
   - `js/game.js`에서 교대 1 ➔ 교대 2 흐름 연결.
4. **디렉터 레이어 (2026-08-10 완성)** — §3-3 참고. 아래 3-2의 시행착오 3이 이 항목이다.
5. **밸런스 검증기 통과 (`tools/validate.mjs`)**
   - 8건 경보에 대해 토큰 소비, NEREUS 정답률, 지배 전략(무조건 기각 등) 방지 검증 PASS.
   - 디렉터 규칙 밸런스 검사도 여기에 추가됨.
5. **제출용 압축 아카이브 생성**
   - `false-positive.zip` (33KB) 생성 완료.

### 3-2. 겪었던 시행착오 및 해결 과정
- **시행착오 1: 토큰 과다 및 NEREUS 무조건 오답 문제 (`validate.mjs` 실패)**
  - *원인*: 초기 경보 추가 시 시간 토큰을 4개 제공하거나 NEREUS 정답률이 0이 되면, 플레이어가 모든 증거를 다 사거나 "AI를 무조건 기각"하는 게 지배 전략이 되는 현상 발생.
  - *해결*: 교대당 토큰을 3개로 타이트하게 제한하고, NEREUS가 맞히는 경보(INC-104, INC-203)를 신뢰도 수치와 다르게 배치하여 플레이어가 NEREUS의 오차 패턴을 직접 생각하도록 밸런스 조정.
- **시행착오 2: 대화 맥락과 파일 치환 오류**
  - *원인*: 대용량 JSON 데이터 편집 시 빈 타겟 교체 에러 발생.
  - *해결*: `write_to_file`을 통해 안전하게 파일 전체 덮어쓰기 방식으로 완전한 구조 보장.
- **시행착오 3: 디렉터 레이어가 '구현됐다'고 기록됐지만 실제로는 죽은 코드였음**
  - *원인*: `applyDirectorRules()`는 있었지만 `S.activePolicies`에 값을 넣는 코드가 어디에도 없었다.
    규칙 선택 UI가 없어 이 함수는 게임 전체에서 **단 한 번도 발동하지 않았다.**
    문서에는 완료로 적혀 있었다 — 코드를 읽지 않고 문서를 믿으면 이런 것을 놓친다.
  - *추가 원인*: 기존 규칙은 `inc.nereus.bias`(숨은 오차 라벨)를 직접 읽고 있었다.
    이건 §2 원칙 위반이다 — 정책이 정답을 훔쳐보면 플레이어가 설계한 게 아니라
    엔진이 대신 푼 것이 된다. 규칙이 절대 빗나가지 않으니 채점할 것도 없어진다.
  - *해결*: §3-3 참고.

## 3-3. 디렉터 레이어 (현재 구조)

- **규칙 테이블: `js/policies.js`** — DOM을 건드리지 않는 순수 ES 모듈.
  게임(`js/game.js`)과 검증기(`tools/validate.mjs`)가 **같은 파일을 공유한다.**
  검증기는 `data:` URL import로 이 `.js`를 ESM으로 강제 로드한다 (package.json·빌드 스텝 불필요).
- **규칙은 관측 가능한 특징만 본다.** `truth`·`nereus.bias` 접근 금지.
  `reporter.state`, `signals[].label`, `nereus.confidence` 등만 사용.
  **이 제약을 절대 풀지 말 것** — 규칙이 정답을 보면 디렉터 레이어의 의미가 사라진다.
- **흐름:** 교대 1 결산 → `restartBtn`("디렉터 콘솔로 →") → `scrPolicy` 화면에서
  4개 중 최대 2개(`POLICY_MAX`) 선택 → `commitPolicy()` → 교대 2에서 실제 적용.
- **채점:** 규칙 발동마다 `S.policyLog`에 `{inc, rule, hit}` 기록.
  `hit` = NEREUS 권고가 실제로 오답이었는가. 최종 결산에 적중/오작동/미발동으로 표시.
- **현재 밸런스 (`node tools/validate.mjs` 출력):**

  | 규칙 | 적중 | 오작동 |
  |---|---|---|
  | 격앙 보고 감산 | 0 | 0 (교대 2에 격앙 보고자 없음 — **의도된 과적합 함정**) |
  | 열수공 감산 | 1 | 0 |
  | 도플러 유보 | 1 | 0 |
  | 고신뢰 일괄 감산 | 2 | 1 (INC-203에서 옳은 권고를 깎음 — **넓은 규칙의 비용**) |

  검증기가 막는 결함: ①오작동이 0이면 "전부 켜기"가 지배 전략 ②적중 규칙이 하나도 없으면
  레이어가 무의미 ③적중 규칙 수가 선택 한도 이하면 정답 조합이 하나로 고정.
  **규칙이나 경보를 수정하면 반드시 `node tools/validate.mjs`를 다시 돌릴 것.**

## 4. 파일 구조

```
c:\coding\my-github-repository\false-positive\
├── index.html                    화면 4개: 인트로 / 경보처리 / 결과 / 결산
├── css/styles.css                심해 관제 콘솔 룩 — CRT 스캔라인, Canvas 소나
├── js/game.js                    게임 코어 (상태, 화면전환, 디렉터 콘솔, 2개 교대 진행)
├── js/policies.js                디렉터 규칙 테이블 — 게임과 검증기가 공유하는 순수 모듈
├── data/incidents.json           베이크드 콘텐츠 (교대 2개, 경보 8건)
├── schema/incident.schema.json   경보 스키마 정의
├── tools/generate.mjs            오프라인 생성기 (OpenAI / Anthropic API 대응 가능)
├── tools/validate.mjs            게임 밸런스 자동 검증기
├── false-positive.zip            OpenAI 해커톤 폼 제출용 압축 파일
├── HANDOFF.md                    인수인계 및 시행착오 문서
├── README.md                     프로젝트 설명서
└── LICENSE
```

## 5. 남은 작업 (8월 18일 마감 전)

1. **OpenAI 웜업 해커톤 폼 제출** (작성된 양식 복사하여 제출하기 버튼 클릭)
2. **시연 영상 제작 (선택)** (30~60초 플레이 캡처 영상 유튜브/드라이브 업로드)
3. **Planescape: Torment 스타일 미니 탐색/액션성 확장 (옵션)**
   - 캔버스 쿼터뷰 맵 기반 ROV 현장 조사 또는 QTE 미니게임 형태 고려 가능.

## 6. 절대 지켜야 할 제약

- **회사 자산·코드·문서·캡처 절대 포함 금지.**
- **빌드 도구(Vite, React 등) 도입 금지.** 순수 HTML/CSS/JS 유지.
- **한국어 UI 텍스트 톤:** 건조하고 구체적인 SF 스타일 유지.
