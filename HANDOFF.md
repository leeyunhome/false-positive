# HANDOFF — 「오탐 / FALSE POSITIVE : THALASSA-9」

이 문서를 읽는 것만으로 지금까지 대화 맥락 없이 작업을 이어받을 수 있어야 합니다. 코드보다 이 문서를 먼저 읽으세요.

## 0. 지금 상황 (가장 중요)

- **대회:** NHN GAME x AI HACKATHON 2026 (NAN2026) — 네이버 아니라 **NHN**.
- **사전과제 마감: 2026-08-10(월) 23:59.** 이 문서 작성 시점(2026-08-07 오후) 기준 **남은 시간 약 3일.**
- **본선: 2026-09-04(금)~09-06(일), NHN 판교 오프라인. 온라인 참여 불가.**
- **상품이 상금이 아니라 "수상자 전원 최종 면접 직행"** — 채용 이벤트. 이게 제작자가 이 대회에 투자하는 진짜 이유입니다. 완성도보다 **"반드시 제출 가능한 상태"** 를 최우선하세요. 못생겨도 끝까지 도는 것 > 예쁘지만 미완성.
- 제작자는 1인 참가, 게임 개발 경험 없음, 본업(임베디드 SW 엔지니어) 병행. **빌드 도구·프레임워크에 대한 낮은 러닝커브를 최우선으로 고려**하세요 — Webpack/Vite/React 등 새 도구 도입 제안은 하지 마세요. 지금 이 스택(순수 HTML/CSS/JS, 빌드 스텝 0)이 의도된 선택입니다.

## 1. 무엇을 만드는가

**장르:** Papers, Please / Reigns / Orwell 계보의 텍스트 기반 의사결정 게임.

**설정:** 수심 3,200m 무인 해저 채굴·연구 기지 THALASSA-9. 야간 당직 관제사(플레이어) 혼자. 온보드 AI **NEREUS**가 경보를 해석해 조치를 권고하지만, **체계적으로(랜덤이 아니라 학습 가능한 패턴으로) 틀린다.**

**코어 루프 (1교대 = 경보 여러 건):**
1. 경보 수신 (승무원 보고 / 소나 / 센서 / 자동 감지)
2. NEREUS 브리핑 — 진단 + 권고 조치 + 신뢰도 수치
3. 플레이어 판단 — 동사 3개만: **승인**(비용 0, NEREUS 권고 그대로 실행) / **조사**(시간 토큰 소모, 하드 증거 확보) / **기각**(직접 다른 조치 선택)
4. 결과 반영 (선체/생명유지/신뢰 게이지 변화) → 다음 경보
5. 교대 종료 → **혼동행렬**(정탐/오탐/미탐/정상기각)로 결산

**아직 미구현인 다음 레이어 (최우선 구현 대상, §5 참고):** 교대 종료 후 NEREUS의 운영 규칙을 자연어로 편집 → 다음 교대에 실제로 그 규칙이 적용됨(프롬프트 주입) → 최종 교대는 플레이어가 손을 떼는 "자율 모드".

## 2. 핵심 설계 원칙 — 절대 어기면 안 되는 것 하나

> **정답(`truth`)은 게임 엔진/콘텐츠 파일이 소유한다. LLM은 "그 정답에 대해 지정된 방향으로 어긋난 조언의 말투와 논리"만 생성한다. LLM이 정답 자체를 결정하게 하면 안 된다.**

이유: LLM이 판정을 하면 (a) 검증 불가능해지고 (b) 같은 입력에 다른 결과가 나와 결정론이 깨지고 (c) 밸런싱이 불가능해집니다. `schema/incident.schema.json`이 이 분리를 강제하는 계약입니다. `truth.isRealThreat`는 콘텐츠 작성 시점에 고정값이고, `nereus.bias`가 그 정답과 얼마나/어떻게 어긋나는지를 지정합니다.

이 원칙을 어기는 방향의 제안(예: "런타임에 LLM이 진단을 실시간으로 판정하게 하자")은 채택하지 마세요.

## 3. 현재 파일 구조 (전부 구현됨, 동작 확인됨)

```
c:\coding\my-github-repository\false-positive\
├── index.html                    화면 4개: 인트로 / 경보처리 / 결과 / 결산
├── css/styles.css                심해 관제 콘솔 룩 — 다크네이비+시안+앰버, CRT 스캔라인, 아트 에셋 0(전부 CSS/Canvas)
├── js/game.js                    게임 코어 전부. 상태(S 객체) · 화면전환 · 판정 · 혼동행렬 · 소나 캔버스 애니메이션
├── data/incidents.json           베이크드(사전생성) 콘텐츠. 현재 교대 1 = 경보 4건(INC-101~104)
├── schema/incident.schema.json   경보 1건의 JSON Schema. §2의 계약을 구조로 강제
├── tools/generate.mjs            오프라인 생성기 — Claude API로 새 경보를 생성 (미실행, 아래 §4 참고)
├── tools/validate.mjs            스키마보다 강한 "게임 규칙" 검증기 (아래 §4 참고)
├── README.md                     플레이어/평가자용 설명
├── LICENSE                       MIT
└── .gitignore
```

Git: `git init` 완료, 첫 커밋(`2a87d82`) 완료. **GitHub 원격 저장소는 아직 생성/푸시 안 됨** — 이게 남은 작업 중 가장 급한 것 중 하나 (§5).

로컬 실행:
```bash
cd c:\coding\my-github-repository\false-positive
python -m http.server 8123
# http://localhost:8123 을 브라우저로 열기
```
`file://`로 직접 열면 안 됨 — `data/incidents.json`을 `fetch()`로 불러오는데 브라우저가 `file://`에서 fetch를 막습니다.

## 4. 두 개의 Node 스크립트 — 반드시 이해하고 갈 것

### `tools/validate.mjs`
`data/incidents.json`을 스키마보다 강한 **게임 밸런스 규칙**으로 검사합니다. 예: "NEREUS 권고가 한 번도 안 맞으면 '무조건 기각'이 지배 전략이 됨" 같은 것. 콘텐츠를 수정할 때마다 반드시 재실행하세요.
```bash
node tools/validate.mjs
```
현재 통과 상태:
```
교대 1: 경보 4 · 실제위협 3 · NEREUS 정답 1 · 토큰 3 (전건 최소조사 4)
✓ 검증 통과
```
**중요한 실화:** 처음 시드 3건(INC-101~103)은 이 검증기에서 실패했습니다 — NEREUS가 한 번도 안 맞아서 "항상 기각"이 지배 전략이 되는 결함, 그리고 토큰이 넉넉해서 긴장이 없는 결함. INC-104(NEREUS가 옳은 경우, 신뢰도는 오히려 0.79로 더 낮게)를 추가하고 토큰을 4→3으로 줄여서 해결했습니다. **경보를 추가/수정할 때마다 이 두 실패 패턴이 재발하지 않는지 이 검증기로 확인하세요.**

### `tools/generate.mjs`
Claude API로 새 경보를 오프라인 생성하는 스크립트. **아직 한 번도 실행된 적 없음** (API 키 미설정 상태로 스캐폴딩만 완료). `--dry-run`으로 프롬프트만 확인 가능:
```bash
node tools/generate.mjs --dry-run
```
실제 생성:
```bash
ANTHROPIC_API_KEY=... node tools/generate.mjs --shift 1 --count 8 --out data/incidents.json
```
이 스크립트의 `planShift()` 함수가 **정답 계획을 결정론적으로 미리 짜고**, LLM에는 "이 정답에 맞는 세계를 쓰라"는 지시만 내립니다(§2 원칙 구현). `validate()` 함수가 생성물마다 §2 원칙 위반 여부(정답 변조, bias 미반영 등)를 검사하고 실패하면 최대 3회 재시도합니다.

**Gemini가 이어받아 할 일 중 하나:** 이 스크립트를 실제로 돌려서 경보를 8건 이상으로 늘리는 것(§5).

## 5. 남은 작업 — 우선순위 순

시간이 절대적으로 부족합니다(3일). **완료 기준을 "제출 가능한 최소 상태"로 잡고, 아래 순서를 지키세요.** 순서를 바꾸면(예: 비주얼 먼저) 마지막 날 제출 실패 위험이 커집니다.

### 5-1. 최우선 — GitHub Pages 배포 확정 (아직 안 됨)
```bash
cd c:\coding\my-github-repository\false-positive
gh repo create false-positive --public --source=. --push
gh api -X POST repos/leeyunhome/false-positive/pages -f "source[branch]=main" -f "source[path]=/"
```
→ `https://leeyunhome.github.io/false-positive/` 로 열리는지 확인. **이걸 먼저 끝내야** 나머지 작업 중 아무거나 실패해도 "일단 도는 제출물"이 확보됩니다.

### 5-2. 콘텐츠 확장 — 경보 4건 → 8건
`tools/generate.mjs` 실행 또는 `data/incidents.json`에 §3 스키마대로 수기 추가. **추가 후 반드시 `node tools/validate.mjs`.** `js/game.js:planShift`가 아니라 `data/incidents.json`의 `shifts[0].incidents` 배열에 id를 추가하는 방식입니다.

### 5-3. 디렉터 레이어 (정책 에디터) — 미구현, 대회 슬로건에 정면 대응하는 핵심 기능
대회 슬로건: *"AI를 활용하는 사람은 많습니다. 우리가 찾는 사람은 AI의 다음 단계를 설계할 디렉터입니다."* 이 게임에서 그 문장에 가장 직접적으로 대응하는 기능이 아직 없습니다. 최소 구현안:
- 교대 종료(결산 화면) 후 텍스트 규칙 3~5개를 체크박스/토글로 제시 (예: "승무원 보고가 격앙 상태면 신뢰도를 -0.2 조정")
- 다음 교대 시작 시 `S.policy` 같은 상태에 선택된 규칙을 저장하고, `js/game.js`의 NEREUS 브리핑 렌더링 로직에서 그 규칙에 해당하는 경보의 `nereus.confidence`/`recommendation`을 규칙에 맞게 실제로 조정
- **자연어 자유 입력 + 실시간 LLM 반영은 시간상 컷 대상.** 사전 정의된 규칙 목록에서 선택하는 방식으로도 "디렉터" 컨셉은 충분히 성립함. 자유 서술은 되면 좋고 안 되면 마는 항목.

### 5-4. 라이브 프록시 (선택, 컷 1순위)
README에 설계는 적어뒀지만 미구현. 서버사이드 프록시(Cloudflare Worker 등)로 API 키 보호 + 레이트리밋 + 실패 시 베이크드로 자동 폴백. **일요일 밤까지 안 되면 즉시 폐기**하고 베이크드 코퍼스만으로 제출.

### 5-5. 제출물 6종 준비 (게임 코드와 별개 트랙, 병행 가능)
| 항목 | 상태 | 메모 |
|---|---|---|
| 플레이어블 빌드+소스 | 코드는 있음, GitHub 미푸시 | §5-1 |
| 플레이 영상 30~60초 | 미착수 | 코어 루프가 돌면 촬영 가능. 자막 하드코딩(나레이션 X) |
| 게임 소개 PDF | 미착수 | HTML 작성 후 Chrome 인쇄 PDF 권장 |
| AI 활용 기술 문서 PDF | 미착수, **승부처** | §2 원칙(정답/조언 분리), `validate.mjs`가 실제로 밸런스 결함을 잡은 사례, 비용/지연/스키마 통과율 실측치를 반드시 포함 |
| 팀 소개 PDF | 불필요 (1인) | |
| 포트폴리오 (선택) | 미착수 | 채용 이벤트이므로 제출 권장 |

## 6. 절대 지켜야 할 제약 (건드리면 안 되는 것들)

- **회사(세연테크) 자산·코드·문서·캡처를 이 프로젝트에 절대 넣지 말 것.** 특히 `edge-monitor`(제작자의 다른 개인 프로젝트, `c:\coding\my-github-repository\edge-monitor`)에 있는 M1706 관련 회사 내부 캡처는 사용 금지.
- **게임 소재를 CCTV/영상보안/공장 관제로 바꾸지 말 것.** 해저로 고정한 이유가 있음(재직사 사업영역과 겹치면 근로계약서상 지식재산권 조항 리스크가 생김). 우주정거장 등 다른 소재로의 변경도 이미 논의 후 기각된 안이니 재제안하지 말 것.
- **README에 명시된 대로 "edge-monitor의 비주얼 언어(UI 패턴)만 재사용, 게임 로직·AI 시스템·콘텐츠는 신규"** 라는 서술을 유지할 것. 실제로 코드를 복붙하지 않았고, 독립적으로 새로 작성됨.
- **빌드 스텝을 도입하지 말 것.** npm 패키지 매니저, 번들러, 프레임워크 전부 불필요. `tools/*.mjs`는 Node 18+ 로컬 실행용 스크립트일 뿐, 게임 런타임과는 무관.
- 커밋 시 git 사용자 정보는 `leeyunhome` / `mszeta@naver.com`(개인 이메일, 회사 이메일 아님)로 유지.
- 한국어 UI 텍스트 톤: 건조하고 구체적. 감탄사·형용사 남발 금지. `js/game.js`의 `result` 문구들이 톤 기준.

## 7. 컷 리스트 (시간 부족 시 이 순서로 버릴 것)

1. 라이브 LLM 프록시 (§5-4)
2. 자율 모드 연출 (교대 완전 자동 진행)
3. 정책 에디터의 자유 서술 입력 (사전 정의 규칙 선택으로 대체)
4. 다중 교대 진행 / 메타 프로그레션 (교대 1개만으로도 제출 가능)

**최소 출하선(이 밑으로는 절대 못 감):** 경보 8건 · 동사 3개 동작 · 혼동행렬 결산 · GitHub Pages 배포 · 제출물 6종 문서.
